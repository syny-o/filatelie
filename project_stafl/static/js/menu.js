//  function toggleResponsiveHeader() {
//      var header = document.querySelector('.main-header');
//      var screenWidth = window.innerWidth;

//      // Add or remove a class based on the screen width
//      if (screenWidth <= 1200) {
//          header.classList.add('responsive-header');
//      } else {
//          header.classList.remove('responsive-header');
//      }
//  }

//  // Add an event listener for the window resize event
//  window.addEventListener('resize', toggleResponsiveHeader);

//  // Initial call to set the initial state on page load
//  toggleResponsiveHeader();